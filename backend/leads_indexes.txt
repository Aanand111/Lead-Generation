[dotenv@17.3.1] injecting env (13) from .env -- tip: ΓÜÖ∩╕Å  load multiple .env files with { path: ['.env.local', '.env'] }
[
  {
    "indexname": "lead_purchases_pkey",
    "indexdef": "CREATE UNIQUE INDEX lead_purchases_pkey ON public.lead_purchases USING btree (id)"
  }
]
